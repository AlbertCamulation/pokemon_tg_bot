name: Update Pokemon Events

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-events:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      pull-requests: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        pip install requests beautifulsoup4

    - name: Run scraper script
      run: |
        python scripts/update_events.py

    - name: Test Secret
      if: env.PAT_TOKEN == ''
      run: echo "Secret is EMPTY!!!"
      env:
        PAT_TOKEN: ${{ secrets.PAT_TOKEN }}

    - name: Create Pull Request
      id: cpr
      uses: peter-evans/create-pull-request@v6
      with:
        token: ${{ secrets.PAT_TOKEN }}
        commit-message: "auto: update event schedule from wingzero"
        committer: GitHub <noreply@github.com>
        author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
        branch: bot-update-events
        base: main
        delete-branch: true
        title: "🤖 自動更新: 活動行程表"
        body: |
          更新 data/events.json 資料
          - 執行者: GitHub Actions
        add-paths: |
          data/events.json

    # ★★★ 修改重點 ★★★
    - name: Auto Merge
      # 只要有 PR 號碼就執行 (不管是新建立的 created 還是更新的 updated)
      if: steps.cpr.outputs.pull-request-number != ''
      # 移除 --auto，改為直接執行合併 (因為你有 Bypass 權限，這樣會強制通過)
      run: gh pr merge --squash --delete-branch "${{ steps.cpr.outputs.pull-request-number }}"
      env:
        GH_TOKEN: ${{ secrets.PAT_TOKEN }}
