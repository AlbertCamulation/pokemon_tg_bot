name: Update Pokemon Events

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-events:
    runs-on: ubuntu-latest
    
    # 這裡只需要讀取權限，寫入權限會由 MY_PAT 提供
    permissions:
      contents: read
      pull-requests: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        pip install requests beautifulsoup4

    - name: Run scraper script
      run: |
        python scripts/update_events.py

    # ★★★ 修改點：移除原本的手動 git push，改用這個 action 自動開 PR ★★★
    - name: Create Pull Request
      id: cpr
      uses: peter-evans/create-pull-request@v6
      with:
        token: ${{ secrets.MY_PAT }}      # 使用你的萬能鑰匙 (代表是你本人操作)
        commit-message: "auto: update event schedule from wingzero"
        committer: GitHub <noreply@github.com>
        author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
        branch: bot-update-events       # 機器人專用的分支
        base: main                      # 目標合併回 main
        delete-branch: true             # 合併後刪除暫存分支
        title: "🤖 自動更新: 活動行程表"
        body: |
          更新 data/events.json 資料
          - 執行者: GitHub Actions
        add-paths: |                    # 指定只提交這個檔案
          data/events.json

    # ★★★ 新增：PR 建立成功後，立刻執行自動合併 ★★★
    - name: Auto Merge
      if: steps.cpr.outputs.pull-request-operation == 'created'
      run: gh pr merge --squash --auto "${{ steps.cpr.outputs.pull-request-number }}"
      env:
        GH_TOKEN: ${{ secrets.MY_PAT }} # 使用你的鑰匙來按「合併」按鈕 (無視規則)
