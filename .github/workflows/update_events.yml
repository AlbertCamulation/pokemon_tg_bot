name: Update Pokemon Events

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-events:
    runs-on: ubuntu-latest
    
    # 基本權限設定 (寫入權限會由 PAT_TOKEN 提供)
    permissions:
      contents: read
      pull-requests: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        pip install requests beautifulsoup4

    - name: Run scraper script
      run: |
        python scripts/update_events.py
    # 加入這個測試步驟
    - name: Test Secret
      if: env.PAT_TOKEN == ''
      run: echo "Secret is EMPTY!!!"
      env:
        PAT_TOKEN: ${{ secrets.PAT_TOKEN }}

    # ★★★ 步驟 1: 自動開 PR ★★★
    - name: Create Pull Request
      id: cpr
      uses: peter-evans/create-pull-request@v6
      with:
        token: ${{ secrets.PAT_TOKEN }}   # <--- 改用你剛剛設定的 Secret
        commit-message: "auto: update event schedule from wingzero"
        committer: GitHub <noreply@github.com>
        author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
        branch: bot-update-events
        base: main
        delete-branch: true
        title: "🤖 自動更新: 活動行程表"
        body: |
          更新 data/events.json 資料
          - 執行者: GitHub Actions
        add-paths: |
          data/events.json

    # ★★★ 步驟 2: 自動合併 ★★★
    - name: Auto Merge
      if: steps.cpr.outputs.pull-request-operation == 'created'
      run: gh pr merge --squash --auto "${{ steps.cpr.outputs.pull-request-number }}"
      env:
        GH_TOKEN: ${{ secrets.PAT_TOKEN }} # <--- 這裡也要改用 PAT_TOKEN
